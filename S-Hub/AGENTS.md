# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

This project is on Expo SDK 57 (React Native 0.86, React 19.2). New Architecture only; Android is edge-to-edge (RN `<StatusBar backgroundColor>` is a no-op there).
